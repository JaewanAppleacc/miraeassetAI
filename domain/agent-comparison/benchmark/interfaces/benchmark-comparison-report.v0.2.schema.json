{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://disclosure-analyst.local/schema/agent-comparison/benchmark/benchmark-comparison-report.v0.2.json",
  "title": "Benchmark Comparison Report v0.2",
  "description": "Turn P6.1: fixes a real contradiction in v0.1 (benchmark-comparison-report.schema.json, PRESERVED UNCHANGED, never modified by this Turn) -- v0.1 `const`-locked holdout_accessed/official_gold_accessed to false unconditionally, even when the underlying DatasetManifest (dataset-manifest.v0.2.json) honestly recorded a REAL, canUseSplit-verified HOLDOUT unlock. holdout_accessed/official_gold_accessed here are real booleans, DERIVED FROM (never independently computed or caller-suppliable apart from) the DatasetManifest that produced this run -- domain/agent-comparison/benchmark/report.mjs's buildComparisonReport reads them directly off the datasetManifest parameter it is given, it does not accept them as separate arguments. ranking_performed remains `const: false` -- this Turn adds provenance honesty, it does not add ranking.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "benchmark_run_id", "dataset_id", "dataset_sha256",
    "dataset_role", "code_revision", "release_id", "release_manifest_sha256",
    "variant_run_results", "identical_item_set", "missing_items_by_variant",
    "model_config_differences", "ranking_performed", "holdout_accessed",
    "official_gold_accessed"
  ],
  "properties": {
    "schema_version": { "const": "0.2.0" },
    "benchmark_run_id": { "type": "string", "minLength": 1 },
    "dataset_id": { "type": "string", "minLength": 1 },
    "dataset_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "dataset_role": {
      "enum": ["SYNTHETIC", "SEED_SMOKE", "DEV_GOLD", "HOLDOUT_GOLD"],
      "description": "Copied verbatim from the DatasetManifest (dataset-manifest.v0.2.json) this run was executed against."
    },
    "code_revision": { "type": "string", "minLength": 1 },
    "release_id": { "type": ["string", "null"] },
    "release_manifest_sha256": { "type": ["string", "null"], "pattern": "^[0-9a-f]{64}$" },
    "variant_run_results": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "https://disclosure-analyst.local/schema/agent-comparison/benchmark/benchmark-run-result.v0.1.json"
      },
      "description": "One embedded BenchmarkRunResult per variant actually compared -- the exact same object report.mjs already produced per variant (BenchmarkRunResult's own schema is unchanged, still v0.1 -- Turn P6.1 does not touch its shape, only ADDS pin-consistency validation before this array is assembled)."
    },
    "identical_item_set": { "type": "boolean" },
    "missing_items_by_variant": {
      "type": "object",
      "additionalProperties": { "type": "array", "items": { "type": "string", "minLength": 1 } }
    },
    "model_config_differences": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["variant_id", "model_config_id", "model_config_sha256"],
        "properties": {
          "variant_id": { "enum": ["STRUCTURED_FIRST", "HYBRID_RETRIEVAL", "PLANNER", "DOCUMENT_FIRST_RAG"] },
          "model_config_id": { "type": "string", "pattern": "^model_[a-z0-9_.-]+$" },
          "model_config_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
        }
      }
    },
    "ranking_performed": { "const": false },
    "holdout_accessed": {
      "type": "boolean",
      "description": "Copied verbatim from the DatasetManifest's own holdout_accessed (dataset-manifest.v0.2.json) -- never independently computed, never a caller-suppliable override."
    },
    "official_gold_accessed": {
      "type": "boolean",
      "description": "Copied verbatim from the DatasetManifest's own official_gold_accessed."
    }
  }
}
