# 런타임 의존성만 — 이 패키지 src/ 트리의 실제 import 그래프 추적(2026-09-06)으로 도출했다.
# 개발 저장소의 잠금 파일을 그대로 복사하지 않았다. 그 저장소의 테스트에만 쓰이는 패키지
# (jsonschema, pytest), 이 패키지에 없는 모듈만 쓰던 패키지(pydantic — 제외된 구 서비스
# 모듈 전용), 이 패키지에 없는 코드 경로용 패키지(tiktoken/beautifulsoup4는 XML 파싱,
# torch/sentence-transformers는 선택 구성 dense 재정렬)는 의도적으로 뺐다.
fastapi==0.141.1
uvicorn==0.52.4
certifi==2026.7.22
